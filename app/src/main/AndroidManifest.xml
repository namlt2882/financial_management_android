<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="project.baonq.menu">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:name="project.baonq.service.App"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="project.baonq.ui.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="project.baonq.ui.LoginActivity" />
        <activity android:name="project.baonq.ui.AddLedgeActivity" />
        <activity android:name="project.baonq.ui.LedgeChoosenActivity" />

        <activity android:name="project.baonq.AddTransaction.AddTransaction">

        </activity>
        <activity android:name="project.baonq.AddTransaction.SelectCategory"></activity>
    </application>

</manifest>